# @typemint/scalars

Environment-agnostic scalar types and utilities for TypeScript.

`@typemint/scalars` has no runtime environment dependencies — it works the same in Node.js,
the browser, and any other JavaScript runtime.

## Install

```bash
pnpm add @typemint/scalars
```

## What's Inside

_This package is a work in progress._
